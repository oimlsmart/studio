# The vendored kernel staging pin (kernel/primmel-1.19.0-pr93-afe9bd5.tgz)
# reports version 1.19.0 while the pinned content package declares the peer
# floor @primmel/primmel >=1.20.0 — unpublished; the tarball IS the
# pre-release 1.20.0 content (the preparation/stimulus constructs). Peer
# resolution must be lenient until the kernel's 1.20.0 publish retires the
# staging pin and this file (kernel/README.md).
legacy-peer-deps=true
