---
title: 'Directory Structure'
shortTitle: 'Directories'
description: Every SMART Recommendation follows the same directory layout. Knowing where each artifact lives makes authoring fast and review tractable.
eyebrow: 'Guide · 03 of 14'
---

import TreeDiagram from '../../../components/diagrams/TreeDiagram.astro'


## Per-standard layout

<TreeDiagram
  caption="FIG. PER-STANDARD DATA LAYOUT"
  accent="brand"
  nodes={[
    { label: 'data/oiml-r60/', kind: 'root', children: [
      { label: 'standard.yaml', annotation: 'identity, value model, terminology' },
      { label: 'dimensions.yaml', annotation: 'classification axes (accuracy, range, etc.)' },
      { label: 'navigation.yaml', annotation: 'app navigation entries' },
      { label: 'enums.yaml', annotation: 'shared enumerations' },
      { label: 'cross-refs.yaml', annotation: 'cross-reference graph' },
      { label: 'entity-relationships.yaml', annotation: 'entity-graph declarations' },
      { label: 'evaluation-profiles.yaml', annotation: 'evaluation workflow profiles' },
      { label: 'formulas.yaml', annotation: 'AsciiMath formula library' },
      { label: 'calculations.yaml', annotation: 'Calculation primitive definitions' },
      { label: 'calculation-context.yaml', annotation: 'binding context for calculations' },
      { label: 'data-classes.yaml', annotation: 'data class declarations' },
      { label: 'data-registries.yaml', annotation: 'data registry declarations' },
      { label: 'gateways.yaml', annotation: 'gateway declarations' },
      { label: 'models/', kind: 'branch', annotation: 'model declarations' },
      { label: 'anr/', kind: 'branch', annotation: 'ANR (additional national requirements)' },
      { label: 'approvals/', kind: 'branch', annotation: 'approval workflow declarations' },
      { label: 'documents/', kind: 'branch', annotation: 'document fragment declarations' },
      { label: 'requirements/', kind: 'branch', children: [
        { label: 'rc.yaml', annotation: 'requirements class schema' },
        { label: 'metrological.yaml', annotation: 'individual requirements' },
        { label: 'technical.yaml' },
        { label: '…', annotation: 'additional domain files' },
      ]},
      { label: 'conformance/', kind: 'branch', children: [
        { label: 'cc.yaml', annotation: 'conformance class schema' },
        { label: 'measurement-error-mdlo.yaml', annotation: 'individual tests' },
        { label: '…', annotation: 'additional test files' },
      ]},
      { label: 'forms/', kind: 'branch', children: [
        { label: 'measurement-error-mdlo.yaml', annotation: 'form schemas' },
        { label: '…', annotation: 'additional form files' },
      ]},
    ]},
  ]}
/>

## Cross-standard layout

<TreeDiagram
  caption="FIG. CROSS-STANDARD LAYOUT"
  accent="teal"
  nodes={[
    { label: 'data/', kind: 'root', children: [
      { label: 'oiml-r60/', annotation: 'one directory per Recommendation' },
      { label: 'oiml-r129/' },
      { label: 'oiml-r144/' },
      { label: 'schemas/', kind: 'branch', annotation: 'JSON Schemas for validation', children: [
        { label: 'requirements.schema.json' },
        { label: 'conformance-test.schema.json' },
        { label: 'form.schema.json' },
        { label: 'standard.schema.json' },
        { label: 'dimensions.schema.json' },
      ]},
      { label: 'platform/', kind: 'branch', children: [
        { label: 'catalog.yaml', annotation: 'document catalog' },
        { label: 'docs/', kind: 'branch', annotation: 'platform documentation YAML', children: [
          { label: 'about.yaml' },
          { label: 'oiml-cs.yaml' },
        ]},
      ]},
    ]},
  ]}
/>

## The generated side

After build, the generated TypeScript modules live under:

<TreeDiagram
  caption="FIG. GENERATED TYPESCRIPT MODULES"
  accent="amber"
  nodes={[
    { label: 'browser/src/data/generated/', kind: 'root', children: [
      { label: 'r60/', kind: 'branch', children: [
        { label: 'requirements.ts', annotation: 'all R 60 requirements' },
        { label: 'conformance.ts', annotation: 'all R 60 tests' },
        { label: 'forms.ts', annotation: 'all R 60 forms' },
        { label: 'standard.ts', annotation: 'R 60 standard + dimensions' },
        { label: 'ontology.ts', annotation: 'R 60 domain ontology' },
      ]},
      { label: 'r129/', kind: 'branch', children: [
        { label: '…', annotation: 'mirrors r60/ structure' },
      ]},
      { label: 'ontology.ts', annotation: 'cross-standard ontology' },
      { label: 'index.ts', annotation: 'manifest of all standards' },
    ]},
  ]}
/>

The application imports these modules at runtime. There is no dynamic loading
of YAML in production, everything is pre-validated TypeScript.

## Conventions

- **One entity per file** when entities are large (forms, tests). Group small entities (terminology entries, enums) in a single file.
- **Naming**: lowercase-kebab-case for files. Camel-case for identifiers (`mpe`, `measurementErrorMdlo`).
- **Cross-references**: always use the URN form (`urn:oiml:requirement:r60:mpe`), never a file path.
- **Comments**: YAML files support `#` comments, use them to record the source clause from the Recommendation (`# R 60 §3.4.1`).

## Next

[Identity & Dimensions](/docs/guides/identity-dimensions) walks through what goes in `standard.yaml` and `dimensions.yaml`.
